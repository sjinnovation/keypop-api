PORT=
MONGO_URI=
JWT_SECRET=
NODE_ENV=
EMAIL_USER=
EMAIL_PASS=
FRONTEND_URL=
# Participant web app (e.g. https://app.keypopapp.org) — email CTAs; falls back to FRONTEND_URL in templates if unset
FRONTEND_URL_WEB=
EXPIRE_TIME=